{
  "name": "linked-list-visualizer",
  "version": "1.0.0",
  "description": "Linked List Visualizer Project",
  "main": "index.html",
  "scripts": {
    "build": "if not exist dist mkdir dist && (if exist css xcopy /E /I /Y css dist\\css) && (if exist js xcopy /E /I /Y js dist\\js) && copy /Y *.html dist && copy /Y README.md dist && copy /Y netlify.toml dist",
    "start": "npx serve ."
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}